/** Управление локальным хранилещем.
 * Так как в localStorage можно добавлять только строки, условимся, что будут записи: ник: кол-во_мух время **/

function addNote(nickname, countFlies, time) {
    window.localStorage()

}